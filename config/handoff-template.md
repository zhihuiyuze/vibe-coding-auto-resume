# HANDOFF.md

> Cross-session continuity file. The `vibe-run` wrapper supports auto-restart after
> rate-limit resets, and the v2 soft-cap monitor will (politely) ask Claude to update this
> file before exhausting the block. A new session starts by reading this; agents should
> end every milestone by updating it.

## Current task

(What is being worked on right now. One paragraph.)

## Completed

(Milestones shipped, with brief notes. Add `[YYYY-MM-DD HH:MM]` timestamps.)

## Next steps

(Ordered list of what to do next, most important first.)

## Blockers

(Open questions, decisions pending from the human, external dependencies stuck.)

## Context

(Key decisions, file paths, commands, configuration choices. The stuff a new agent needs
to make sense of the codebase without re-reading every PR.)
